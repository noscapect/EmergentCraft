import "dotenv/config";
import { OllamaProvider, resolveOllamaModel } from "./provider.js";
import { PerceptionSchema, validateDecision } from "./contracts.js";
const baseUrl = process.env.OLLAMA_BASE_URL ?? "http://127.0.0.1:11434";
const model = await resolveOllamaModel(baseUrl, process.env.OLLAMA_MODEL ?? "");
const perception = PerceptionSchema.parse({ schemaVersion:1, agentId: "4bde43d4-cbe1-49d0-8a33-9b2e8004881d", name: "Smoke", epoch: 1, capabilities:{playerHunger:true,inventory:true,nativeNavigation:true,blockBreaking:true,itemPickup:true,eating:true,equipment:true,combat:true,crafting:true,placement:true}, self: { position: { x: 0, y: 64, z: 0 }, health: 20,maxHealth:20,alive:true,onGround:true,inWater:false,onFire:false,airSupply:300,fallDistance:0,effects:[],inventory:[],hunger:{current:20,max:20,saturation:5,starving:false},equipment:{mainHand:null,offHand:null,head:null,chest:null,legs:null,feet:null} }, environment: { dimension: "minecraft:overworld", timeOfDay: 6000, weather: "clear", light: 15, biome: "minecraft:plains",daytime:true,temperature:0.8,downfall:0 }, entities: [], blocks:[],items:[], events: [], candidates: [{ id: "wait:1", kind: "WAIT", description: "Wait briefly and observe." }] });
const started = performance.now(); const decision = validateDecision(await new OllamaProvider(baseUrl, Number(process.env.OLLAMA_REQUEST_TIMEOUT_MS ?? 120000), process.env.OLLAMA_KEEP_ALIVE ?? "15m").decide(perception, { working:[],recalled:[],social:[],knowledge:[],beliefs:[],reflections:[],projects:[],chapters:[],self:{intent:"",updatedAt:new Date(0).toISOString()},contextChars:0 }, {model,profile:"generic"}), perception);
console.log(JSON.stringify({ model, latencyMs: Math.round(performance.now() - started), decision, schemaValid: true }, null, 2));
